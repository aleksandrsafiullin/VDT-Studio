"use client";

/**
 * @deprecated Use ExecutionModeSettings in the settings modal instead.
 * Kept as a thin re-export during the execution-mode settings transition.
 */
export { ExecutionModeSettings as AiProviderSettings } from "./execution-mode-settings";
