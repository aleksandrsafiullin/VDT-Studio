{
  "name": "@vdt-studio/cli",
  "version": "0.1.0-alpha.0",
  "private": false,
  "type": "module",
  "exports": {
    ".": "./dist/index.mjs"
  },
  "bin": {
    "vdt": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "@vdt-studio/local-runner": "workspace:*",
    "@vdt-studio/vdt-core": "workspace:*",
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "pnpm clean && esbuild src/index.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/index.mjs && esbuild src/bin.ts --bundle --platform=node --format=esm --target=node24 --outfile=dist/cli.mjs && node -e \"require('node:fs').chmodSync('dist/cli.mjs',0o755)\"",
    "start": "tsx src/bin.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. packages/cli/src"
  }
}
