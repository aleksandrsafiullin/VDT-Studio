// Historical command retained as a compatibility entry point. The original
// 13-file freeze has been superseded by the deterministic gzip transport
// verifier, which preserves and verifies the canonical uncompressed identity.
import "./verify-sequence-3-vector-transport.mjs";
